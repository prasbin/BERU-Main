# BERU development & test dependencies.
# Install with:  pip install -r requirements-dev.txt
# (includes everything in requirements.txt)

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4

"""BERU Alembic migration scripts.

Packaged so ``pip install .`` ships ``migrations/`` alongside the application
(the installed layout keeps ``backend/database/migrations.py`` resolving this
directory via ``Path(__file__).parents[2]``).
"""
"""Project-local release tooling (version bump, lockfile, pre-release gate).

Run as: ``python -m scripts.release <command>`` (see ``check --help`` /
``bump --help`` / ``lock --help``). The full procedure is documented in
``docs/release.md``.
"""